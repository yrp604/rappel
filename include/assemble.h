#include <stdint.h>

const
size_t assemble_intel(
		uint8_t *const,
		const size_t,
		const char *const,
		const size_t);

const
size_t assemble_arm(
		uint8_t *const,
		const size_t,
		const char *const,
		const size_t);
